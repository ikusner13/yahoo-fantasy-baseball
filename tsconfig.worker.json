{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "types": ["@cloudflare/workers-types"]
  },
  "include": ["alchemy.run.ts", "src/**/*.ts", "src/**/*.tsx"],
  "exclude": ["node_modules", "tests", "scripts", "repos", "legacy", "dist"]
}
